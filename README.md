# an implementation for neural network using feed forward and back probagation in a TDD style
# the code is vectorized for better GPU utilization. 
# the number of layers and neurons in eaech layer is dynamic as well as the activation function.
